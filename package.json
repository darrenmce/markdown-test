{
  "name": "markdown_test",
  "version": "1.0.1",
  "description": "Converts a markdown file into a test html file via github api and the github flavoured css",
  "main": "markdown-test.js",
  "bin": {
    "mdtest": "bin/mdtest"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "open": "0.0.5",
    "prompt": "^0.2.14",
    "request-promise": "^0.3.3",
    "temp": "^0.8.1"
  }
}
